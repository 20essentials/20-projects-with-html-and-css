<section class='bg'>
  <aside class='fondin'></aside>
  <h2>
    20 PROJECTS WITH HTML & CSS

    <br />
    <article class='icons-container'>
      <img class='html' src='./assets/html.svg' alt='html logo' />
      <img class='css' src='./assets/css.svg' alt='css logo' />
    </article>
  </h2>
</section>

<style>
  .icons-container {
    display: flex;
    justify-content: center;
    gap: 1.4vmax;
    margin-top: 1.5vmax;

    img {
      width: 3.5vmax;

      &.css {
        width: 4vmax;
      }
    }
  }

  .bg {
    display: flex;
    flex-wrap: wrap;
    height: 100vh;
    background-color: black;
    background-image: url('./assets/fondo.avif');
    background-position: center;
    background-size: cover;
    position: relative;

    .fondin {
      z-index: 9999;
      mix-blend-mode: overlay;
      inset: 0;
      position: absolute;
      animation: moveTitle 10s ease infinite alternate;
    }

    h2 {
      position: absolute;
      font-family: var(--font-bubble);
      color: #e9e9e9;
      font-size: 3vmax;
      top: 50%;
      left: 50%;
      text-wrap: balance;
      width: 35%;
      transform: translate(-50%, -50%);
      text-align: center;
    }
  }

  @keyframes moveTitle {
    0% {
      background-color: #f0f;
    }

    25% {
      background-color: #0f0;
    }

    50% {
      background-color: #00f;
    }

    75% {
      background-color: #f00;
    }

    100% {
      background-color: #ff0;
    }
  }
</style>
